# Normalize text line endings in the repo; leave working copies alone.
* text=auto

# Treat these as binary (no line-ending munging, no diff noise)
*.xlsm binary
*.xlsx binary
*.docx binary
*.zip  binary
*.pdf  binary
*.gwbk binary
